isoloader can console custom from Zapi
- this version is a customized version of can console, for normal application, both normal version and custom version are needed
- the custom version added level 2 authentication's function such as changing encoder's offset parameter, transferring parameters between motor controllers (ble4)